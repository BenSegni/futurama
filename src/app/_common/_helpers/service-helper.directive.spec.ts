import { ServiceHelperDirective } from './service-helper.directive';

describe('ServiceHelperDirective', () => {
  it('should create an instance', () => {
    const directive = new ServiceHelperDirective();
    expect(directive).toBeTruthy();
  });
});
